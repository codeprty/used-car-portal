package com.example.usedcarportal.model;

// Enum representing user roles in the system.
public enum Role {
    USER,  // Regular user role
    ADMIN  // Administrator role with additional privileges
}
